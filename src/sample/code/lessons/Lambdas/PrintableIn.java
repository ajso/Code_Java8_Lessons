package sample.code.lessons.Lambdas;

//Note: When an interface has one abstract method, It's called a FUNCTIONAL interface.
public interface PrintableIn {

    //Note: When a method inside an interface has no implementation like this one. it's called an ABSTRACT method.
    //Method-1
    void print(String Suffix);
}
